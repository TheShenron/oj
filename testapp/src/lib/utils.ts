import type { Product } from '../types/types';

export function filterProducts(
    products: Product[],
    query: string,
    selectedCategories: string[]
): Product[] {
    // TODO: implement filtering logic
    // Candidate must return filtered array

    return [];
}
